/* Number of overseer worker threads. Should match number of CPU cores reported in /proc/cpuinfo. */
#define NUM_OF_WORKER 5
/* Number of reader threads.  Should match number of CPU cores reported in /proc/cpuinfo. */
#define NUM_NETWORK_READER 4

